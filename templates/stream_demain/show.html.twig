{% extends 'base.html.twig' %}

{% block title %}StreamDemain{% endblock %}

{% block body %}
    <h1>StreamDemain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stream_demain.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stream_demain_index') }}">back to list</a>

    <a href="{{ path('app_stream_demain_edit', {'id': stream_demain.id}) }}">edit</a>

    {{ include('stream_demain/_delete_form.html.twig') }}
{% endblock %}
